apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: matchering
  namespace: flux
spec:
  interval: 10m0s
  path: "./flux/matchering/manifests"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux
  postBuild:
    substituteFrom:
    - kind: ConfigMap
      name: common-configs
