apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: coredns
  namespace: flux
spec:
  interval: 10m0s
  path: "./flux/coredns/yaml"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux
